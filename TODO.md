# TODO

1. ~~CREATE endpoint that AI can access what has the places the user had been and all the info so chat bot has idea on what user info and can personalized its response:~~ ✅ Done — `GET /ai/context` returns the full travel profile, and `/ai/chat` now injects it into the system prompt automatically.